# Nginx URL重写配置

将以下配置添加到Nginx的server块中，确保所有分类和单词详情页URL能正确访问：

```nginx
server {
    listen 80;
    server_name mzc0603.xyz;
    root /var/www/html/english-phonetic;
    index index.html;

    # 分类页路由：/category/* -> index.html
    location ~ ^/category/ {
        try_files $uri $uri/ /index.html;
    }

    # 单词详情页路由：/word/* -> word.html
    location ~ ^/word/ {
        try_files $uri $uri/ /word.html;
    }

    # 首页重定向到/category/all
    location = / {
        return 301 /category/all;
    }

    # 静态资源缓存
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 其他请求
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

## 配置说明

1. **分类页路由**：所有 `/category/*` 请求都由 `index.html` 处理
2. **单词详情页路由**：所有 `/word/*` 请求都由 `word.html` 处理
3. **首页重定向**：访问根路径自动跳转到 `/category/all`
4. **静态资源缓存**：CSS、JS、图片等资源缓存30天

## 应用配置

```bash
# 测试配置
sudo nginx -t

# 重载配置
sudo nginx -s reload
```

## URL格式示例

| 功能 | URL格式 |
|------|---------|
| 全部分类 | https://mzc0603.xyz/category/all |
| 日常问候 | https://mzc0603.xyz/category/richangwenhou |
| 情感表达第2页 | https://mzc0603.xyz/category/qingganbiaoda?page=2 |
| 单词详情 | https://mzc0603.xyz/word/hello |
| 单词详情 | https://mzc0603.xyz/word/thank-you |
